# Codex Ink contributor rules

Read README and docs/eink-ui-spec.md before editing. Preserve the documented APIs, hook schemas, device protocol and native 400×300 layout. The public baseline uses fictional data; do not overwrite it without documenting and validating an intentional visual change. Run targeted tests and packaging checks. Never include personal account data, credentials or device identifiers in fixtures or reports.
